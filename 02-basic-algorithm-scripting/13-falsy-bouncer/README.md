# <a href="https://www.freecodecamp.org/challenges/falsy-bouncer">Task Details</a>

Remove all falsy values from an array.

Falsy values in JavaScript are `false`, `null`, `0`, `""`, `undefined`, and `NaN`.
